| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| ユーザー「解決済みなのにまだ返信できます。これはおかしい」 | 解決済み会話への追記を防ぐ | `status == "resolved"` のグローバル会話は履歴だけを表示し、返信入力・画像添付・返信ボタンを持たない | 状態 | 会話を解決した後 | 解決済みグローバル会話とは、履歴を読めるが新しい発言を追加できない会話を指す | resolved global conversation |
| ユーザー「新規作成できるようにして。グローバルコメントとして」 | 解決済み会話と別の論点を開始する | 解決済みグローバル会話の下で本文または画像を送信し、新しい未解決の `scope: "round"` コメントを追加する操作 | 事象 | 解決済み会話を表示した後、新しい論点を入力して送信する | グローバルコメント作成とは、既存会話へ返信せず新しい未解決会話を追加する操作を指す | create global comment |
| 既存 `r-<round>` と同一ラウンド内の追加作成 | 新旧グローバル会話を一意に識別する | 現在ラウンド番号と、そのラウンドで既に存在する `scope: "round"` コメント数から作る永続ID | 値 | 作成要求を受けた後、保存と通知の前 | グローバルコメントIDとは、同一ラウンド内で各グローバル会話を一意に指す永続値を指す | `r-<round>-<sequence>` |
| 既存 `/reply-comment` とHerdr通知 | 人間の新しい論点をAIへ即時に届ける | 新しいグローバルコメントを `review.json` へ保存し、同じIDを含むHerdr通知とSSE更新を送る処理 | 手段 | ID生成と保存の後、画面更新とAI応答の前 | グローバルコメント作成APIとは、新しいグローバル会話の保存・通知・画面更新を一度に行うHTTP操作を指す | `/create-global-comment` |
| 既存の画像返信保存処理と新規グローバルコメント | 返信と新規作成で同じ添付保存規則を使う | data URLの画像をレビュー用添付ディレクトリへ保存し、review.json用の相対参照を返す処理 | 手段 | 返信または新規作成の入力検証後、review.json保存の前 | 会話画像保存とは、会話に添付された画像を永続ファイルへ変換して相対参照を返す処理を指す | `save_conversation_image` |
| ユーザー「驚き最小のUIになってない」 | チャットを開いた直後に次の発言方法を理解できるようにする | 最新グローバル会話が解決済みなら、履歴より前に新規作成フォームを表示する | 状態 | 右下チャットを展開した直後 | 新しい会話とは、解決済み履歴へ追記せず、別の未解決グローバル会話を開始する入力領域を指す | 新しい会話 |
| 同上 | 終了状態をチャット全体の主状態と誤認させない | 解決済みグローバル会話を既定で閉じ、必要な時だけ展開して読む履歴領域 | 状態 | 新しい会話の入力領域より後 | 過去の会話とは、返信できない解決済みグローバル会話の履歴をまとめた折りたたみ領域を指す | 過去の会話 |
