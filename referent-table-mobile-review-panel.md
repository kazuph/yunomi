| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| スマホ添付画像と「サイド…も被ってます」 | PC版サイト表示でもスマホ向け配置を選ぶ | CSSレイアウト幅が980pxでも、端末画面幅が既存モバイル境界の960px以下である条件 | 開始条件 | 端末幅を判定してからデスクトップ列を解除する | 端末幅条件とは、レイアウト幅ではなく端末画面幅が既存の960px境界以下であることを指す | `(max-device-width: 960px)` |
| 「facebookのmessengerみたいに、右下に縮小可能」 | 本文幅を奪わず保存済み会話を読めるようにする | グローバル会話を文書列の外に出し、画面右下へ固定して表示している状態 | 状態 | レビュー状態の取得後に右下へ表示し、既存の保存状態に応じて展開または縮小する | 右下チャット表示とは、グローバル会話を本文・サムネイル・サイドバーの列計算から外して画面右下へ固定した状態を指す | 既存の `review-loop-sidebar` |
| 同上 | スマホで本文を隠さず会話の存在を示す | 端末幅条件を満たし、展開を明示保存していない時の40px縮小状態 | 状態 | 初回表示では縮小し、利用者が展開すると保存済み状態を更新する | スマホ初期縮小状態とは、スマホで未設定のグローバル会話を既存の40px縮小表示から開始する状態を指す | 既存の `review-loop-sidebar-collapsed` |
| 既存サイドバー最小化操作 | 新しい操作体系を増やさず会話を開閉する | 右下チャット内の既存最小化ボタンで展開状態と縮小状態を切り替える操作 | 事象 | 保存済み状態の描画後に操作可能になり、押下のたびに表示状態とlocalStorageを更新する | 右下チャット表示切替とは、既存最小化ボタンで右下チャットの展開と縮小を切り替える操作を指す | 既存の toggle 処理 |
