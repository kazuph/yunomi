| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| ユーザー指示「対話できない起動方法を完全に禁止」 | 人間の入力を起動元エージェントへ必ず返す | yunomi がコメント、チェックボックス、判定を受け付け、起動元エージェントの現在paneへ通知する一連のレビュー | 目的 | yunomi起動前に通知先を確定し、起動後に人間が入力する | 対話可能なレビューとは、人間の入力が起動元エージェントへ即時通知されるレビューを指す | 対話可能なレビュー |
| `herdr pane current` と `herdr pane get` | 通知先の誤配・欠落を防ぐ | 現在セッションに対応するpane IDを実行時に取得し、正当なpaneとして検証できたこと | 開始条件 | yunomiコマンドを実行する前 | 通知先paneの証明とは、現在paneを実行時に取得して検証できたことを指す | 通知先paneの証明 |
| yunomi `--notify-pane` | 人間の入力を起動元へ配送する | 証明済みpane IDをyunomiプロセスへ明示的に渡すコマンド引数 | 手段 | 通知先paneの証明後、サーバー起動時 | 明示通知先とは、`--notify-pane`へ渡した証明済みpane IDを指す | 明示通知先 |
| `herdr run` | Codexへ終了結果を確実に戻す | yunomiプロセスの終了通知と保存ログを起動元paneへ配送する起動経路 | 手段 | 明示通知先を付けたyunomiコマンドの外側 | 通知安全なCodex起動とは、`herdr run`で終了通知を受け、同時にyunomiへ明示通知先を渡す起動を指す | 通知安全なCodex起動 |
| 現行skillとrepo内起動例 | 通知不能なレビューを開始させない | `--notify-pane`が無い、pane IDを推測する、通知先を証明できない、またはCodexが直接foreground/backgroundで起動する全コマンド | 状態 | 起動前検査で一つでも該当した時 | 起動禁止状態とは、人間の入力または終了結果を起動元へ返せる証明がない状態を指す | 起動禁止状態 |
| yunomi起動後の確認 | 設定漏れを起動直後に検出する | サーバー稼働と、起動コマンドに証明済み `--notify-pane` が含まれることを一度確認した状態 | 状態 | 起動成功後、人間のレビュー開始前 | 対話経路確認済みとは、サーバー稼働と明示通知先の両方を確認した状態を指す | 対話経路確認済み |
