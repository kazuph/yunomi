# 長い未配置コメントを右下チャット内でスクロールできるようにする

## 今回の依頼

> 「スクロールもなんもできなくなっている。動作チェックして修正して。」
>
> 「文字のフォントも大きさもweightも違うのでは？？？」
>
> 「吹き出し上部のhumanは消していいよ。両方ね。」
>
> 「これ早く統一してね。枠線も違うのか・・・。」
>
> 「投稿すると一番上まで戻っちゃうのね。だめなので直してね。」

## Why

重複防止後の `Not shown in document` セクションには縦スクロール領域がなく、長い会話が固定高さの右下チャットから切れて、下端の返信欄へ到達できなかった。また、`Reply` はブラウザ標準のボタン書体を継承し、`Resolve conversation` の12px・600とはフォント、大きさ、weightが一致していなかった。

## How

- 右下チャット全体と固定返信欄は動かさず、`Not shown in document` セクションだけを残り高さ内の縦スクロール領域にする。
- `Reply` と `Resolve conversation` は共通のボタン規則を使い、高さ、padding、枠線、角丸、背景、文字色、フォント、12px、weight 600を揃える。
- 吹き出し上部の `human` / `agent` は両方とも削除し、左右位置と背景色だけで話者を区別する。
- 投稿前に末尾を見ていた会話は再描画後も最新投稿へ追従し、過去ログを読んでいた会話は同じ位置を維持する。

## What

- 長い未配置会話はマウスホイールで先頭から末尾へ移動でき、下端の返信欄まで到達できる。
- `Reply` と `Resolve conversation` は枠線を含む同じボタン外観で表示される。
- human側とagent側のどちらの吹き出しにも話者ラベルは表示されない。
- human投稿とagent返信のどちらも、投稿直後に一番上へ戻らず最新内容が見える。
- 最新ビルドのローカル成果物から新しいレビュー画面を起動し直す。

## 検証

- 長い未配置会話30行を使い、`overflow-y: auto`、`scrollHeight > clientHeight`、マウスホイールによる `scrollTop` の増加、末尾到達、返信欄全体の表示を重点ブラウザE2Eで確認した。
- 話者ラベルが0件であることと、`Reply` / `Resolve conversation` のcomputed height・padding・border・radius・background・color・fontが一致することを重点E2Eで確認した。
- 初回の最新表示、末尾でのhuman投稿、末尾でのagent返信、履歴閲覧中の位置維持を重点E2Eで確認した（job `job-1785919555434-1980-419`）。`send_now.ts` も20/20成功した。
- 最終ソースでMoonBit 190/190と登録済み全V2 E2Eが成功した（job `job-1785919606245-1980-422`）。
- 添付画像のwrapperを話者ラベルと誤認しない判定へ直し、重点E2Eが成功した（job `job-1785920155054-1980-445`）。
- round 27のChrome実画面でhuman/agent文字ラベル0件、`Reply` / `Resolve conversation` のcomputed style 11項目一致、右下チャットへのホイール入力で `scrollTop` が0から31へ変化することを確認した。確認後は通常viewportへ戻した。

commit・push・PR・npm公開は実施しない。
