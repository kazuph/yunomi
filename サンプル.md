# yunomi v3 サンプル

これは v3 PLAN の各 Task Card 完了時に、実装した worktree の yunomi で開くための確認用 Markdown です。

## チェック項目

- Markdown が通常表示される
- コメント対象の行が残る
- 変更ファイル自動検出の後でも既存の `yunomi <file>` が壊れていない

## 2.1 auto-changes

`yunomi review [base-ref]` は git の変更ファイルを集め、1 つのサーバープロセス内で `?f=N` による切替 UI を表示します。
