# yunomi v3 サンプル

これは v3 PLAN の各 Task Card 完了時に、実装した worktree の yunomi で開くための確認用 Markdown です。

## チェック項目

- Markdown が通常表示される
- コメント対象の行が残る
- 変更ファイル自動検出の後でも既存の `yunomi <file>` が壊れていない

## 2.1 auto-changes

`yunomi review [base-ref]` は git の変更ファイルを集め、1 つのサーバープロセス内で `?f=N` による切替 UI を表示します。

## 2.2 html-preview

`*.html` ファイルは sandboxed iframe 内で描画され、対象 DOM 要素への selector / bounds 付きコメントを YAML に出力します。

## 2.3 code-diff

Diff 画面はファイルツリー、Unified / Split 切替、viewed checkbox を持ち、表示モードと viewed 状態を localStorage に保存します。

## 2.4 send-now

コメントカードの Save 隣に Send now が表示され、クリックすると最終 Submit 前でも agent へ即時通知されます。agent reply は同じページのコメント一覧へ inline 表示され、`review.json` の履歴にも残ります。

## 2.5 mcp-bridge

`yunomi mcp` は stdio MCP サーバーとして起動し、`yunomi_review_state` / `yunomi_add_comment` / `yunomi_go` ツールで review 状態の取得、コメント追加、次ラウンド制御を公開します。

## 2.6 structured-context

Submit YAML にはコメントの対象行だけでなく、前後行 `context` と保存済み添付画像の `image_path` も含まれます。agent は review.json / YAML から修正対象を探さずに特定できます。
