| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| ユーザーのスクリーンショット | AI返信を元の指摘の直下で読めるようにする | `data-comment-key` が返信IDと一致する青い即時コメントカード | 目的 | 人間が即時コメントを送信 → AIが返信 → 同じ青いカードに表示 | 即時コメントカードとは、本文またはソースの該当箇所直下に出る `Line 11` 形式のカードを指す | `.yunomi-inline-comment[data-comment-key]` |
| 既存SSE `reply` イベント | ページを再読み込みせず返信を反映する | `apply_comment_reply_event` が受け取った返信を、コメント一覧と即時コメントカードの両方へ追加する処理 | 事象 | AIが返信 → SSE `reply` を受信 → 2つの表示面を更新 | 既存事象を再利用 | `apply_comment_reply_event` |
| 既存localStorage | 再読み込み後も返信を復元する | `yunomi:comment-replies:<filename>` に保存されたコメントID別の返信配列 | 記録 | SSE受信時に保存 → 再読み込み → 保存済みカード描画後に復元 | 既存記録を再利用 | `comment reply store` |
| 既存返信表示 | 返信の見た目を一覧と即時コメントカードで揃える | 返信者・ラウンド・本文を並べる既存の返信リスト | 手段 | 保存済みコメント描画 → 対応する返信配列を挿入 | 既存表示を再利用 | `.comment-inline-replies` |
